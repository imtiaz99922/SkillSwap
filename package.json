{
  "name": "skillswap",
  "version": "1.0.0",
  "description": "SkillSwap - A platform for skill sharing and learning",
  "scripts": {
    "build": "cd client && npm install && npm rebuild && npm run build",
    "dev": "npm run dev --prefix client",
    "install-all": "npm install && cd client && npm install && cd ../backend && npm install && cd ../api && npm install"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "workspaces": [
    "client",
    "backend",
    "api"
  ]
}
